name: Test
on: push
jobs:
  Test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Setup
        uses: actions/setup-node@v5
        with:
          node-version: 20
      - name: install
        run: npm install
      - name: Test
        run: npm test
